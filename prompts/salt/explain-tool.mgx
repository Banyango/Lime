---
description: "the description of the addition tool"
---
import files

@struct AdditionTool:
    result: int

@fn load_files() int:
    var file = files.load("data.txt")

    for line in file.lines() {
        <<
        process line: ${line}
        >>
    }

if description > 10 {
    << You are an addition tool ${description} >>
}

@effect emit
